// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   output: "export",
//   reactStrictMode: true,
// };

// module.exports = nextConfig;

// module.exports = {
//   images: {
//     domains: ["www.bimaabazar.com"],
//   },
// };
/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  output: 'export',
  reactStrictMode: true,
  images: {
    domains: ["www.bimaabazar.com"],
  },
}
 
module.exports = nextConfig